{
  "name": "focus-port-root",
  "private": true,
  "scripts": {
    "build": "cd focusport-frontend && npm install --no-audit --no-fund && npm run build",
    "test": "vitest"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "gsap": "^3.14.2"
  }
}
